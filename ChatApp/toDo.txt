
-----------SERVER-------------

=> a chaque demande de connexion:
    -> instancier un socket de communication
    -> le sauvegarder dans une liste

=> pour chaque client connecté:
    -> instancier un thread
        > il intercepte les messages envoyés par le client associé
        > il les transfert à tous les autres clients en utilisant les
          sockets enregistrés dans la liste


-----------CLIENT-------------

=> etablir une connexion avec le server

=> lorsqu'il est connecté:
    -> envoi du pseudo de l'utilisateur
    -> accès au salon de discussion

    -> le client doit pouvoir envoyer des messages et en recevoir
    -> pour se deco, il envoi un message au server (deAuth request)
        > le server informe tout le monde de la deconnexion


------------IDEES-------------

=> Le serveur possede une hashmap avec les sockets associés à une clé (un pseudo)
    -> quand le client se connecte, le serveur verifie si le pseudo n'est pas
       deja dans la hashmap, et confirme la connexion dans ce cas

=> override toString pour afficher un ChatObjet correctement

=> commandes de chat

=> regler le probleme de header AC qui eviterai de creer un ois a chaque fois...

=> Une page de connexion au demarrage, qui quand on se connecte ouvre la ChatBox

=> regler le probleme de la bulle qui ne change pas de taille en fonction de celle è
